{
  "id": "vfox-elixir",
  "version": "0.3.1",
  "name": "Elixir (via vfox)",
  "documentationURL": "https://github.com/ccavolt/devcontainer-features/tree/main/src/vfox-elixir",
  "description": "Elixir is a functional, concurrent, general-purpose programming language that runs on the BEAM virtual machine which is also used to implement the Erlang programming language.",
  "installsAfter": [
    "ghcr.io/ccavolt/devcontainer-features/vfox",
    "ghcr.io/ccavolt/devcontainer-features/vfox-erlang"
  ],
  "options": {
    "version": {
      "default": "latest",
      "description": "Select the version of Elixir to install.",
      "proposals": [
        "latest",
        "1.14.5-otp-26"
      ],
      "type": "string"
    },
    "locale": {
      "default": "en_US.UTF-8",
      "description": "Select the locale you want to use. Setting locale prevents Elixir warnings",
      "proposals": [
        "en_US.UTF-8",
        "fr_FR.UTF-8"
      ],
      "type": "string"
    },
    "defaultMixCommands": {
      "default": "no",
      "description": "Installs hex, rebar, and phx_new by default.",
      "proposals": [
        "no",
        "yes"

      ],
      "type": "string"
    }
  }
}
